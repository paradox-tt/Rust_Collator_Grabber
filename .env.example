# Collator Monitor Environment Variables
# ========================================
# Copy this to .env and fill in your values.
# Environment variables override config.toml settings.
#
# Variable naming: 
#   COLLATOR_ prefix + field name in SCREAMING_SNAKE_CASE
#   Single underscores are preserved in field names
#   Use double underscore (__) for nested config (e.g., chains)
#
# Examples:
#   polkadot_collator_address → COLLATOR_POLKADOT_COLLATOR_ADDRESS
#   proxy_seed               → COLLATOR_PROXY_SEED
#   chains.polkadot_assethub.rpc_url → COLLATOR_CHAINS__POLKADOT_ASSETHUB__RPC_URL

# Collator addresses (SS58 format)
# Your Polkadot collator account (used for all Polkadot system chains)
COLLATOR_POLKADOT_COLLATOR_ADDRESS=1YOURPOLKADOTADDRESSHERE1234567890123456789

# Your Kusama collator account (used for all Kusama system chains)
COLLATOR_KUSAMA_COLLATOR_ADDRESS=CYOURKUSAMAADDRESSHERE1234567890123456789012

# Proxy account seed (mnemonic or hex)
# This account signs transactions on behalf of the collator.
# It should be configured as a NonTransfer proxy on-chain.
# IMPORTANT: Keep this secret! Use restrictive file permissions (chmod 600)
COLLATOR_PROXY_SEED=your twelve or twenty four word mnemonic phrase goes here

# Slack webhook for notifications (optional - remove if not using Slack)
COLLATOR_SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Slack user IDs to ping for actionable events (comma-separated, optional)
# Find member IDs by clicking on a user's profile in Slack → More → Copy member ID
# Example: U08CUCTA3R7,U12345ABCD
COLLATOR_SLACK_USER_IDS=U08CUCTA3R7

# Check interval in seconds (for watch mode, default: 3600 = 1 hour)
COLLATOR_CHECK_INTERVAL_SECS=3600

# Summary interval in seconds (for periodic status summary, default: 21600 = 6 hours)
COLLATOR_SUMMARY_INTERVAL_SECS=21600

# Log level (trace, debug, info, warn, error)
RUST_LOG=info
